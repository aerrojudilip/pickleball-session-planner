{
  "name": "pickleball-session-planner",
  "version": "1.0.0",
  "description": "Static, cloud-backed Pickleball Session Planner using Supabase. Players, fair scheduling, scores, statistics, court bookings, and JSON backup on GitHub Pages.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node scripts/serve.js",
    "serve": "node scripts/serve.js",
    "test": "npm run test:unit",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
